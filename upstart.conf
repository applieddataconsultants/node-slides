#!upstart

description "node-slides-uwec"
author      "wavded"

start on (local-filesystems and net-device-up IFACE=eth0)
stop on shutdown

respawn
env NODE_ENV=production

script
   exec /usr/local/bin/node /var/www/node-slides-uwec/app.js >> /var/log/node/node-slides-uwec.log 2>&1
end script
